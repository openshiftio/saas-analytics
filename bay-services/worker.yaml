services:
- &worker_def
  hash: dc2ecc9e36bafe953a844e2a5356f3db9133821c
  hash_length: 7
  name: worker-ingestion
  environments:
  - name: production
    parameters:
      WORKER_ADMINISTRATION_REGION: ingestion
      REPLICAS: 6
      WORKER_EXCLUDE_QUEUES: GraphImporterTask
  - name: staging
    parameters:
      WORKER_ADMINISTRATION_REGION: ingestion
      REPLICAS: 2
      WORKER_EXCLUDE_QUEUES: GraphImporterTask
  path: /openshift/template.yaml
  url: https://github.com/fabric8-analytics/fabric8-analytics-worker/

- <<: *worker_def
  name: worker-api
  environments:
  - name: production
    parameters:
      WORKER_RUN_DB_MIGRATIONS: 1
      WORKER_ADMINISTRATION_REGION: api
      WORKER_EXCLUDE_QUEUES: GraphImporterTask
      REPLICAS: 4
  - name: staging
    parameters:
      WORKER_RUN_DB_MIGRATIONS: 1
      WORKER_ADMINISTRATION_REGION: api
      WORKER_EXCLUDE_QUEUES: GraphImporterTask
      REPLICAS: 2

- <<: *worker_def
  name: worker-ingestion-graph-import
  environments:
  - name: production
    parameters:
      WORKER_ADMINISTRATION_REGION: ingestion
      WORKER_INCLUDE_QUEUES: GraphImporterTask
      WORKER_NAME_SUFFIX: '-graph-import'
  - name: staging
    parameters:
      WORKER_ADMINISTRATION_REGION: ingestion
      WORKER_INCLUDE_QUEUES: GraphImporterTask
      WORKER_NAME_SUFFIX: '-graph-import'

- <<: *worker_def
  name: worker-api-graph-import
  environments:
  - name: production
    parameters:
      WORKER_ADMINISTRATION_REGION: api
      WORKER_INCLUDE_QUEUES: GraphImporterTask
      WORKER_NAME_SUFFIX: '-graph-import'
  - name: staging
    parameters:
      WORKER_ADMINISTRATION_REGION: api
      WORKER_INCLUDE_QUEUES: GraphImporterTask
      WORKER_NAME_SUFFIX: '-graph-import'
